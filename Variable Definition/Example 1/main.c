#include <stdio.h>

int main() {
    double k, m = 3.0, x, y = 4.0, z;
    printf("\nVariable Values\n-----------------------");
    printf("\nk=%e\nm=%e\nx=%e\ny=%e\nz=%e\n", k, m, x, y, z);

    int a, b, c;
    a = b = c = 5;
    printf("\na = %d, b = %d, c = %d\n", a, b, c);
    return 0;
}

